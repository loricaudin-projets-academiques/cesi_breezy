events {}

http {
    client_max_body_size 10M;

    upstream api_upstream {
        server api:3000;
    }

    server {
        listen 80;

        location /api/ {
            proxy_pass http://api_upstream/;
        }
    }
}

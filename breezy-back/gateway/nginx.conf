events {}

http {
    upstream api_upstream {
        server api:3000;
    }

    server {
        listen 80;

        location /api/ {
            proxy_pass http://api_upstream/;
        }

        location /uploads/ {
            proxy_pass http://api_upstream/uploads/;
        }
    }
}
